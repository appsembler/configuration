# {{ ansible_managed }}

{% for name,value in item.items() -%}
{%- if value|default(None) != None and value != "" -%}
export {{ name }}="{{ value }}"
{% endif %}
{%- endfor %}

eval "$(rbenv init -)"
